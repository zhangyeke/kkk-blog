generator client {
  provider = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  // output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["auth", "public"] // <-- 关键：添加此行
}
// 在这里可以放置一些全局的 enum 或 type   @supabase/ssr